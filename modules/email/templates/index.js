const { getHead } = require('./head');
const { getStyles } = require('./styles');
const { getContent } = require('./content');
const { getBody } = require('./body');
const { getFooter } = require('./footer');

module.exports = { getHead, getStyles, getBody, getContent, getFooter };
